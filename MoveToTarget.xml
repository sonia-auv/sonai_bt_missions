<?xml version="1.0" encoding="UTF-8"?>
<root BTCPP_format="4">
  <BehaviorTree ID="MoveToTargetTree">
    <IfThenElse>
      <RetryUntilSuccessful num_attempts="5">
        <CheckAiInfo max_count="1"
                     ai_info="{target}"
                     confidence="0.800000"
                     camera="0"
                     min_count="1"
                     classification="{classification}"/>
      </RetryUntilSuccessful>
      <Sequence>
        <InitTraj trajectory="{traj}"/>
        <MoveToTargetTree trajectory="{traj}"
                          offset="{offset}"
                          aiObj="{target}"/>
        <SendTrajToPlanner trajectory="{traj}"
                           interpolation="0"/>
      </Sequence>
      <AlwaysFailure/>
    </IfThenElse>
  </BehaviorTree>

  <!-- Description of Node Models (used by Groot) -->
  <TreeNodesModel>
    <Condition ID="CheckAiInfo">
      <input_port name="max_count"
                  default="1"
                  type="int"/>
      <output_port name="ai_info"
                   type="AiDetectionArray"/>
      <input_port name="confidence"
                  default="0.800000"
                  type="float"/>
      <input_port name="camera"
                  default="0"
                  type="int">0: Front, 1: Bottom</input_port>
      <input_port name="min_count"
                  default="1"
                  type="int"/>
      <input_port name="classification"
                  type="std::string"/>
    </Condition>
    <Action ID="InitTraj">
      <output_port name="trajectory"
                   type="Trajectory"/>
    </Action>
    <Action ID="MoveToTargetTree">
      <inout_port name="trajectory"
                  type="Trajectory"/>
      <input_port name="offset"
                  type="float"/>
      <input_port name="aiObj"
                  type="AiDetection"/>
    </Action>
    <Action ID="SendTrajToPlanner">
      <inout_port name="trajectory"
                  type="Trajectory"/>
      <input_port name="interpolation"
                  default="0"
                  type="int"/>
    </Action>
  </TreeNodesModel>

</root>
